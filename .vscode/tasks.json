{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "lilypond",
            "type": "shell",
            "command": "docker run --rm -v ($PWD.Path + ':/app') -w /app/SeniorHymnal iskaron/lilypond lilypond Hymnal.ly",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}